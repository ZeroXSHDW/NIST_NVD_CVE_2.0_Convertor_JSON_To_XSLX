# Reproducible CI dependency input. Refresh requirements-ci.txt with:
#   uv pip compile requirements-ci.in --python-version 3.12 --universal --generate-hashes --output-file requirements-ci.txt
-r requirements.txt
pytest==9.0.3
pip-audit==2.10.1
